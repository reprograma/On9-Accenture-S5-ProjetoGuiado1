function calculate(operation) {
    /* 
        Variável operation contém as operações que o usuário digitou 
        na calculadora. Utilize essa variável como ponto de partida
        para lógica da sua calculadora.
        Em sua função retorne o resultado que será mostrado em tela.
    */

    return 0;
}